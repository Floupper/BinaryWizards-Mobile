import { StyleSheet } from "react-native";

export const styleInput = StyleSheet.create({
    input: {
        width: "100%",
        borderWidth: 1,
        borderRadius: 5,
        fontSize: 18,
        textAlign: "center",
        borderColor: "black"
    }
});
