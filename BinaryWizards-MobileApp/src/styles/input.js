import { StyleSheet } from "react-native";

export const styleInput = StyleSheet.create({
    input: {
        height: 50,
        width: "90%",
        margin: 12,
        borderWidth: 1,
        borderRadius: 10,
        fontSize: 18,
        textAlign: "center",
        borderColor: "black"
    }
});
